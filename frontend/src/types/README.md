# Shared frontend types

`geometry.ts` defines the canonical version-1 construction document and runtime
evaluation values. The JSON field names intentionally match the Pydantic aliases
in `backend/app/geometry/models.py`.
