# Symbolic backend

This package will wrap allowlisted SymPy parsing, simplification, solving, limits,
and stable result serialization. API routers should depend on this service rather
than importing SymPy directly.
